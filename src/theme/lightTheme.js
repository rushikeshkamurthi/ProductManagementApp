import {MD3LightTheme} from 'react-native-paper';

export const lightTheme = {
  ...MD3LightTheme,
  colors: {
    ...MD3LightTheme.colors,
    primary: '#007bff',
    background: '#ffffff',
    surface: '#f8f9fa',
  },
};

export default lightTheme; // Export as default
